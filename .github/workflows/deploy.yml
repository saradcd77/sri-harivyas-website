name: Deploy to AWS Lightsail

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ashram-website/package-lock.json

      - name: Install dependencies
        working-directory: ./ashram-website
        run: npm ci

      - name: Build application
        working-directory: ./ashram-website
        run: npm run build

      - name: Deploy to Lightsail
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.LIGHTSAIL_HOST }}
          username: ${{ secrets.LIGHTSAIL_USERNAME }}
          key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          port: ${{ secrets.LIGHTSAIL_PORT }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}
            
            # Pull latest changes
            git pull origin main
            
            # Install dependencies
            npm install --production
            
            # Build application
            npm run build
            
            # Restart PM2 application
            pm2 restart ashram-website || pm2 start npm --name "ashram-website" -- start
            
            # Save PM2 configuration
            pm2 save
            
            echo "✅ Deployment completed successfully!"
            echo "राधेकृष्ण राधेकृष्ण कृष्णकृष्ण राधेराधे 🙏"

